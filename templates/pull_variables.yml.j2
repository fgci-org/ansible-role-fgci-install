---
# variables for ansible-pull
# 
#
pull_install_ip: "{{ kickstart_server_ip }}"
pull_login_ip: "{{ http_proxy }}"

{% raw %}
ntp_config_server: [ "{{ pull_install_ip }}", "10.1.1.1" ]
collectd_network_server_host: "{{ pull_install_ip }}" 
{% raw %}
adminMailAddr: "{{ adminMailAddr }}"

