---

 - hosts: localhost
   remote_user: root
   roles:
     - ansible-role-fgci-install
   vars:
     - adminMailAddr: "test-email@example.com"
     - kickstart_server_ip: "10.1.1.2"
     - int_gateway: "10.1.1.4"
     - hosts_file_src: "tests/inventory"
     
